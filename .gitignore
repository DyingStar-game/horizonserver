Horizon
/.vs
/.vscode

# will have compiled files and executables
debug/
target/
RecipeSmith/

/.cargo
# Cargo.lock is intentionally committed: this workspace builds binaries/plugins
# and the lock pins a compatible libwebrtc/webrtc-sys pair (drift breaks the FFI).
/tests/node_modules
